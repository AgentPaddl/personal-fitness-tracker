-r requirements.txt
pytest>=8,<9

# Only required to run tests/test_end_to_end.py, which imports the gateway's
# FastAPI app in-process (via httpx ASGI transport) to exercise the real
# backend -> gateway -> FakeProvider path without any network server.
-r ../ai-gateway/requirements.txt
