# Server-side values only; never ship these in the iOS application.

# Fails closed by default: the food-analysis route returns 403 unless this
# is explicitly "development". No production authentication exists yet.
APP_ENV=development

AI_GATEWAY_BASE_URL=http://127.0.0.1:8000
AI_GATEWAY_TIMEOUT_SECONDS=10
